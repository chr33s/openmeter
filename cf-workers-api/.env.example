# Environment Configuration Example
# Copy to .env.local for local development

# API Configuration
API_KEY_SECRET=your-secure-api-key-secret-here
JWT_SECRET=your-jwt-secret-here
JWT_ISSUER=openmeter
JWT_AUDIENCE=openmeter-api

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=100
RATE_LIMIT_BURST=20

# Caching
CACHE_TTL_SECONDS=300
IDEMPOTENCY_TTL_HOURS=24

# AI Configuration
DEFAULT_AI_MODEL=@cf/meta/llama-3.1-8b-instruct

# Database Configuration (automatically configured in wrangler.toml)
# D1_DB=binding-name
# KV_CACHE=binding-name

# Environment
ENVIRONMENT=development